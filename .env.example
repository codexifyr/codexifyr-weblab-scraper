# Codexifyr currently requires no API keys for normal local operation.
# Add future local-only configuration keys here by NAME only.
# Never commit real credentials into this file.

CODEXIFYR_HOST=127.0.0.1
CODEXIFYR_PORT=8877
